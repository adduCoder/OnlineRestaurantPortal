package com.orders.util;

/**
 * Enum representing the roles within the application.
 * It defines the different types of roles a user can have.
 */
public enum Role {

  /**
   * Represents a regular user with standard permissions.
   */
  USER,

  /**
   * Represents an owner with elevated permissions.
   */
  OWNER
}
